import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    headerPrice: {
        width: "100%",
        height: "auto",
        alignItems: "center",
        marginBottom: 20
    },
    currentPrice: {
        color: '#FFB829',
        fontSize: 32,
        fontWeight: "bold",
        paddingTop: 20
    },
    textPrice: {
        color: "#998356",
        fontSize: 16,
        fontWeight: "bold", 
    }
})

export default styles