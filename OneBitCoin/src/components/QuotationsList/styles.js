import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    filters: {
        width: '100%',
        flexDirection: 'row',
        paddingVertical: 15,
        justifyContent: 'space-evenly'
    },
    buttonQuery: {
        width: 50,
        height: 30,
        backgroundColor: '#CCA049',
        borderRadius: 10,
        alignItems: 'center',
        justifyContent: 'center'
    },
    textButextButtonQuery: {
        color: '#665E4E',
        fontWeight: 'bold',
        fontSize: 14,
    }
})

export default styles